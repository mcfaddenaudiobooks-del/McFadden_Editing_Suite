# Core AI & Logic
numpy
huggingface_hub
faster-whisper>=1.0.3
# For NVIDIA GPU acceleration (Windows), install PyTorch with CUDA BEFORE running pip install:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121


# NVIDIA CUDA 12 Support (Windows only — macOS uses CPU automatically)
nvidia-cublas-cu12; sys_platform == "win32"
nvidia-cudnn-cu12==9.*; sys_platform == "win32"

# Audio Processing
librosa
soundfile
soxr
audioread
pooch
audioop-lts

# Legacy Support (Python 3.13+)
standard-aifc
standard-chunk
standard-sunau